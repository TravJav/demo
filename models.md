
## context of scope 

We are a payment rails system, we process different requests from different vendors that count on us for providing vacation packages to their clients. We do not process fraud information/ checks this is processed before our rails. We can assume all requests coming to our service are cleared.


Expected data input from the client

{
amount_minor
currency (uppercase ISO 4217 supported by configured processors)
line_item ( item id)
psp_ref: ( uuid4 generated by us for standardization )

}


## Models


Transaction


Ledger specific to the transaction. Ledger should never be deleted or updated, only appended.
Ledger rows are signed money movements:

- charge entries are positive
- refund entries are negative
- adjustment entries are non-zero

Transaction stores the payment lifecycle and original processor reference. Ledger stores
movement-specific processor references, such as a refund id.

ProcessorAttempt records each charge or refund attempt against a vendor rail, including failed and
refused attempts that do not create money movements.

IdempotencyRecord stores inbound request hashes and replay responses for charge/refund requests so
duplicate submissions do not duplicate ledger movements.


vacation
id
package_name
vacation_package FK


flights
id
name
flight_number
reference_number
seat
name
vacation_package FK.vacation


hotels
id
name
booking_number
reference_number
vacation_package FK.vacation


processor_attempts
id
transaction_id FK.transaction
processor
operation charge|refund
status succeeded|failed|refused
processor_reference
error_code
error_message
created_at


idempotency_records
id
idempotency_key unique
request_path
request_hash
status_code
response_body
transaction_id FK.transaction nullable
created_at




## Product context:

You are a product engineer at VGS. Our design-partner customer is Atlas Voyages, a travel 
aggregation platform that sells airline tickets, hotel stays, and tours & experiences from suppliers 
worldwide all in one checkout.
Their payment traffic is currently regional to the US and EU, but they are expanding to east Asia as 
well.
1. Atlas processes cards through different payment vendors in different regions each with its own 
API, its own formats, and its own failure modes. Every new vendor is a months-long manual 
integration project for Atlas's engineers who were hired to build travel software, not payments 
plumbing.
2. Travel inventory is perishable. A fare hold or room rate lives for minutes; when a charge fails at 
one vendor, the booking usually dies with it. Atlas wants failed charges automatically retried 
through another vendor before the fare expires.
3. Refunds and reporting - due to cancelled flights, changed itineraries, weather. Atlas's finance 
team can't answer "how much did we take today, net of refunds, across all vendors and 
currencies?" without stitching together several vendor dashboards and CSV exports.
Atlas has asked VGS to solve this once, for good: one connection. Atlas integrates a single 
standard API for charges and refunds. Behind it, VGS routes each payment to the right vendor, fails 
over when a vendor is down, and keeps an accurate ledger of every movement of money. The goal 
is that Atlas's engineers never hand-configure a vendor integration again.
Atlas was able to provide API specs for each of these vendors, but they don’t have actual accounts 
that can be shared. Nevertheless, you are responsible to build the MVP of that product
